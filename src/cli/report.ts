/**
 * Deliverable 2.  pnpm report
 *
 * Prints the reconciliation report. What it counts is up to you.
 *
 * This is a stub.
 */
import { env } from '../config/env';

async function main(): Promise<void> {
  void env;

  console.error('report: not implemented.');
  console.error('Implement this in src/cli/report.ts.');
  process.exit(1);
}

void main();
