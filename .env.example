# Copy to .env before running anything: cp .env.example .env

# Matches the credentials in compose.yaml.
DATABASE_URL="postgresql://postgres:postgres@localhost:55432/agent_feed?schema=public"

# The static key GET /agents/:npn expects from callers.
API_KEY="local-development-key"

PORT=3000
